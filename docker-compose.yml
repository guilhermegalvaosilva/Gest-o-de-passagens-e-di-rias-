services:
  nugb-gereb-form:
    build: .
    container_name: nugb-gereb-form
    ports:
      - "3002:3002"
    environment:
      PORT: 3002
      CORS_ORIGIN: "*"
    volumes:
      - nugb-gereb-data:/app/data
    restart: unless-stopped

volumes:
  nugb-gereb-data:
